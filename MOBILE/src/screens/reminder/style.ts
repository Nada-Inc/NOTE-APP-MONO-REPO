import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    margin: 20,
  },
  dateTimePicker: {
    flexDirection: "row",
    gap: 6,
  },
  reminderTextField: {
    marginTop: 10,
  },
});

import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    margin: 20,
  },
  settingsTitle: {
    fontWeight: "bold",
  },
  settingSingleItem: {
    flexDirection: "row",
    gap: 15,
    marginTop: 20,
    alignItems: "center"
  },
  radioTheme: {
    flexDirection: "row",
    alignItems: "center"
  }
});
